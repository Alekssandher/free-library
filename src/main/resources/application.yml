spring:
  servlet:
      multipart:
        max-file-size: 50MB
        max-request-size: 50MB
  application:
    name: free-library-api
  datasource: 
    url: ${DB_URL}
    username: ${DB_USER}
    password: ${DB_PASSWORD}
  jpa:
    hibernate:
      ddl-auto: validate
      open-in-view: false